
name: rive
version: 0.13.20+1 # was 0.13.20-3
homepage: https://rive.app
description: Rive Flutter Runtime. This package provides runtime functionality for playing back and interacting with animations built with the Rive editor available at https://rive.app.
repository: https://github.com/rive-app/rive-flutter

publish_to: none

# topics:
  #   - animation
  #   - ui
  #   - effects
  #   - widgets
  #   - widget
#   -
environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=2.5.0"

dependencies:
  collection: ^1.15.0
#  ffi: ^2.1.4
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  http: ">=0.13.3 <2.0.0"
  meta: ^1.3.0
  plugin_platform_interface: ^2.0.2
  rive_common: # swappable
    # path: ../rive-common
    git:
      url: git@github.com:kaypora-games/rive-common.git
      ref: main

  stokanal: # swappable
    # path: ../stokanal-flutter/
    git:
      url: git@github.com:kaypora-games/stokanal-flutter.git
      ref: master

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail:
  path: ^1.8.3
